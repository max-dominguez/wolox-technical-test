import React, { FunctionComponent } from 'react';

import styles from './SignUpBanner.module.scss';

export const SignUpBanner: FunctionComponent = () => (
  <section role="complementary" className={styles.signup_banner_container} />
);
